import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
  // Replace the text below with something that actually appears in your App component
  // For example, if your App renders "Hello World", use that text instead
  // expect(screen.getByText(/Hello World/i)).toBeInTheDocument();
});
